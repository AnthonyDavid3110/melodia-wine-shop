/** Restrained closing footer — identity only, no fabricated contact/social links. */
export function PublicFooter() {
  return (
    <footer className="border-border mt-auto border-t px-5 py-8 sm:px-8">
      <p className="text-muted-foreground mx-auto max-w-5xl font-sans text-caption uppercase">
        Ensemble de Cuivres Mélodia
      </p>
    </footer>
  );
}
